<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Lista contactos</title>
</head>

<body>
    <div class="container">
        <div class="d-flex justify-content-end">
            <a class="mt-3 btn btn-primary" href="form">
                Agregar Contacto
            </a>
        </div>


        <h1>Lista Contactos</h1>
        <% if(contactos.length>0) { %>

            <div class="table-responsive">
                <table class="table table-striped table-hover table-responsive-md">
                    <thead class="table-dark">
                        <tr>
                            <th>Nombre</th>
                            <th>Número</th>
                            <th>Dirección</th>
                            <th>E-mail</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i=0; i<contactos.length;i++) { %>
                            <tr>
                                <td>
                                    <%= contactos[i].nombre%>
                                </td>
                                <td>
                                    <%= contactos[i].numero%>
                                </td>
                                <td>
                                    <%= contactos[i].direccion%>
                                </td>
                                <td>
                                    <%= contactos[i].email%>
                                </td>
                                <td>
                                    <button class="btn btn-danger "><a class="text-light text-decoration-none" href="/eliminar/<%=contactos[i].id%>">Eliminar</a></button>
                                    <button class="btn btn-warning"><a  class="text-light text-decoration-none" href="/editar/<%=contactos[i].id%>">Editar</a></button>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>

                </table>
            </div>
            <% } else { %>
                <div>sin Contactos</div>
                <% } %>
    </div>
    </div>

</html>